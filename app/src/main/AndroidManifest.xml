<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    xmlns:tools="http://schemas.android.com/tools">

    <uses-permission android:name="android.permission.POST_NOTIFICATIONS" />
    <!-- Live Updates API 所需权限 -->
    <uses-permission android:name="android.permission.POST_PROMOTED_NOTIFICATIONS" />
    <uses-permission android:name="android.permission.QUERY_ALL_PACKAGES" tools:ignore="QueryAllPackagesPermission" />
    <uses-permission android:name="android.permission.INTERNET" />
    <uses-permission android:name="android.permission.ACCESS_NETWORK_STATE" />
    <uses-permission android:name="android.permission.ACCESS_WIFI_STATE" />
    <uses-permission android:name="android.permission.NEARBY_WIFI_DEVICES" />
    <!-- 触觉反馈：用于超级岛关闭指示器的振动效果 -->
    <uses-permission android:name="android.permission.VIBRATE" />
    <!-- 蓝牙名称获取相关权限 -->
    <uses-permission android:name="android.permission.BLUETOOTH" />
    <uses-permission android:name="android.permission.BLUETOOTH_ADMIN" />
    <!-- Android 12+ 需声明 BLUETOOTH_CONNECT -->
    <uses-permission android:name="android.permission.BLUETOOTH_CONNECT" />
    <uses-permission android:name="android.permission.PACKAGE_USAGE_STATS" tools:ignore="ProtectedPermissions" />

    <!-- MIUI/澎湃专用应用列表权限声明 -->
    <uses-permission android:name="com.android.permission.GET_INSTALLED_APPS" />
    <!-- 悬浮窗权限：用于在其他应用上层显示悬浮窗（超级岛复刻等功能需要） -->
    <uses-permission android:name="android.permission.SYSTEM_ALERT_WINDOW" />
    <!-- 修复后台服务前台启动崩溃 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE" />
    <!-- Android 14+ 特殊前台服务权限声明 -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_SPECIAL_USE" />

    <!-- Android 14+ 数据同步前台服务权限声明（解决 FGS type dataSync 崩溃） -->
    <uses-permission android:name="android.permission.FOREGROUND_SERVICE_DATA_SYNC" />
    <!-- 文件管理权限 -->
    <uses-permission android:name="android.permission.MANAGE_EXTERNAL_STORAGE" tools:ignore="ProtectedPermissions" />
    <uses-permission android:name="android.permission.READ_EXTERNAL_STORAGE" />
    <uses-permission android:name="android.permission.WRITE_EXTERNAL_STORAGE" />
    <!-- 剪贴板访问权限 -->
    <uses-permission android:name="android.permission.READ_CLIPBOARD_IN_BACKGROUND" android:maxSdkVersion="34" tools:ignore="ProtectedPermissions" />
    <!-- 日志读取权限，用于检测剪贴板变化 -->
    <uses-permission android:name="android.permission.READ_LOGS" tools:ignore="ProtectedPermissions" />

    <application
        android:enableOnBackInvokedCallback="true"
        android:allowBackup="true"
        android:dataExtractionRules="@xml/data_extraction_rules"
        android:fullBackupContent="@xml/backup_rules"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/Theme.NotifyRelay">
        <activity android:name=".ui.GuideActivity"
            android:exported="true">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />
                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity android:name=".ui.MainActivity" android:exported="true" />
        <!-- 通知监听服务声明，用于系统识别通知访问权限 -->
        <service
            android:name=".servers.NotifyRelayNotificationListenerService"
            android:label="@string/app_name"
            android:permission="android.permission.BIND_NOTIFICATION_LISTENER_SERVICE"
            android:exported="true"
            android:foregroundServiceType="specialUse"
            tools:ignore="MissingClass">
            <intent-filter>
                <action android:name="android.service.notification.NotificationListenerService" />
            </intent-filter>
        </service>
        <!-- 注册通知点击广播接收器 -->
        <receiver
            android:name=".feature.device.receiver.RemoteNotificationClickReceiver"
            android:exported="false" />
        
        <!-- 剪贴板同步广播接收器 -->
        <receiver
            android:name=".servers.clipboard.ClipboardSyncReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.xzyht.notifyrelay.CLIPBOARD_MANUAL_SYNC" />
                <action android:name="com.xzyht.notifyrelay.CLIPBOARD_OPEN_SETTINGS" />
            </intent-filter>
        </receiver>
        
        <!-- 超级岛通知广播接收器，用于处理通知删除事件 -->
        <receiver
            android:name=".feature.notification.superisland.NotificationBroadcastReceiver"
            android:exported="false">
            <intent-filter>
                <action android:name="com.xzyht.notifyrelay.ACTION_CLOSE_NOTIFICATION" />
                <action android:name="com.xzyht.notifyrelay.ACTION_TOGGLE_FLOATING" />
            </intent-filter>
        </receiver>
        
        <!-- 剪贴板监控服务 -->
        <service
            android:name=".servers.ClipboardMonitorService"
            android:exported="false"
            android:foregroundServiceType="dataSync"
            android:stopWithTask="false" />
            
        <!-- 剪贴板同步透明Activity -->
        <activity
            android:name=".ui.ClipboardSyncActivity"
            android:exported="true"
            android:theme="@style/TransparentTheme"
            android:launchMode="singleInstance"
            android:noHistory="true"
            android:excludeFromRecents="true"
            android:taskAffinity="" />
            
        <!-- 开发者模式Activity -->
        <activity
            android:name=".ui.DeveloperModeActivity"
            android:exported="true"
            android:theme="@style/Theme.MaterialComponents.DayNight.NoActionBar" />
            
        <!-- 剪贴板无障碍服务 -->
        <service
            android:name=".servers.ClipboardAccessiblityService"
            android:label="NotifyRelay 设备互联"
            android:permission="android.permission.BIND_ACCESSIBILITY_SERVICE"
            android:exported="false">
            <intent-filter>
                <action android:name="android.accessibilityservice.AccessibilityService" />
            </intent-filter>
            <meta-data
                android:name="android.accessibilityservice"
                android:resource="@xml/clipboard_accessibility_service" />
        </service>
    </application>

</manifest>