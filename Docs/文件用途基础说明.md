# Notify-Relay-android 项目结构与文件用途说明

> 本文基于当前仓库实际代码校验整理（`:app` 版本 `2.169.0` / `versionCode 10329325`）。
> 若后续重构目录或模块，请同步更新本文档。

---

## 一、项目概览

### 1.1 项目信息

| 项 | 值 |
|---|---|
| 项目名（rootProject） | `NotifyRelay` |
| 仓库根目录 | `Android/`（工作区 `e:\GitHub-code\Notify-Relay\Android`） |
| 应用 ID / `:app` namespace | `com.xzyht.notifyrelay` |
| compileSdk / targetSdk | 37 / 37 |
| `:app` minSdk | 31（Android 12） |
| `:base` / `:data` / `:superislandui` / `:checkupdata` minSdk | 29（取 `libs.versions.minSdk`） |
| `:core` minSdk | 29（源码目录 `nativecore/`） |
| `:scrcpy` minSdk | 26（子模块自带配置） |
| 模块数量 | 7 个 |
| 主源码 `.kt` 文件数 | 约 317 个（不含测试，不含 `checkupdata`/`scrcpy` 两个 Git 子模块） |

### 1.2 模块清单

| 模块 | namespace | 说明 | 是否 Git 子模块 |
|---|---|---|---|
| `:app` | `com.xzyht.notifyrelay` | 主应用（UI、业务 feature、同步） | 否 |
| `:base` | `notifyrelay.base` | 基础工具库（无内部模块依赖；原 `:core`/`notifyrelay.core` 工具类已并入） | 否 |
| `:data` | `notifyrelay.data` | 数据层（Room / 配置 / 持久化，依赖 `:base`） | 否 |
| `:superislandui` | `github.xzynine.superislandui` | 超级岛（SuperIsland）悬浮 UI 渲染 | 否 |
| `:checkupdata` | `github.xzynine.checkupdata` | 更新检查 | 是 |
| `:scrcpy` | `io.github.miuzarte.scrcpyforandroid` | Scrcpy 投屏 | 是 |
| `:core` | `com.xzyht.notifyrelay.nativecore` | Rust 核心 FFI 绑定（JNA），依赖 `:base`；模块名占 `:core`，源码目录为 `nativecore/` | 否（内含 `notify-relay-core` 子模块） |

> 模块命名说明：`:core` 现指真正的 Android 原生核心（Rust FFI）。原先占用该名的
> `notifyrelay.core` 工具库（7 个工具类）已整体并入 `:base`，包名由
> `notifyrelay.core.util[.image]` 改为 `notifyrelay.base.util[.image]`。
> 为免牵连 `.gitmodules` 中的 `nativecore/notify-relay-core` 子模块路径，
> 该模块**只改 Gradle 模块名与依赖坐标，目录仍为 `nativecore/`**（`settings.gradle.kts`
> 中以 `project(":core").projectDir = file("nativecore")` 重定向）。

> LSPosed 模块（超级岛鉴权绕过）已拆分为**独立仓库 `NotifyRelay-LSP`** 与独立 APK
> （`applicationId = com.xzyht.notifyrelay.lsp`），不再作为主应用模块参与构建。
> 原先 `:lsp` 作为 library 随主应用打包，导致主应用每次重装都触发其热重载并造成卡顿。

> 子模块来源（`.gitmodules`）：
> - `checkupdata` → `git@github.com:xzy-nine-common/checkupdata-android.git`（branch `main`）
> - `scrcpy` → `git@github.com:xzy-nine/ScrcpyForAndroid.git`（branch `library`）
> - `nativecore/notify-relay-core` → `https://github.com/NotifyRelay/core.git`（branch `main`）
>
> 根 `build.gradle.kts` 对 `checkupdata`/`scrcpy` **不应用** ktlint，子模块代码在其各自仓库维护。

### 1.3 模块依赖关系

```
:app ──┬── :data ─────────── :base
       ├── :base
       ├── :superislandui ─┬ :base
       │                   └── :data ── :base
       ├── :scrcpy ────────┬ :base
       │                   └── :data ── :base
       ├── :core ─────────── :base      （Rust FFI，源码目录 nativecore/）
       ├── :checkupdata        （无内部依赖）
       └── :base
```

`buildSrc/` 为 Gradle 构建辅助工程，`:core` 之外还有 Rust 侧代码（位于 `nativecore/notify-relay-core`）。
LSPosed 模块已移至平级独立仓库 `NotifyRelay-LSP/`（不参与本仓库构建）。

---

## 二、各模块目录结构

### 2.1 `:app` 模块（208 个 .kt）

包根：`app/src/main/java/com/xzyht/notifyrelay/`

```
├── util/
│   └── ApkArchMatcher.kt                 APK 架构匹配（用于更新包选择）
│
├── sync/                                 传输与协议层
│   ├── ConnectionDiscoveryManager.kt     局域网设备发现
│   ├── ConnectionKeepAlive.kt            连接保活
│   ├── FtpServer.kt                      FTP 服务（图标/文件传输）
│   ├── HeartbeatProcessor.kt             心跳处理
│   ├── MessageSender.kt                  消息发送门面（转发总入口，保持外部 API 不变）
│   ├── SendGateway.kt                    发送网关（反射取已认证设备 + 通知入队封装）
│   ├── builder/                          消息构造与发送实现拆分
│   │   ├── MediaMessageBuilder.kt        媒体全量/播放/结束 消息
│   │   ├── SuperIslandMessageBuilder.kt  超级岛 全量/数据/结束 消息
│   │   └── ChatMessageBuilder.kt         聊天测试/普通通知转发 消息
│   ├── ProtocolSender.kt                 协议发送封装
│   └── notification/
│       ├── NotificationProcessor.kt      通知协议处理
│       ├── StatusProcessor.kt            状态协议处理
│       ├── SuperIslandProcessor.kt       超级岛协议处理
│       └── data/
│           ├── NotificationAction.kt
│           ├── NotificationRecord.kt
│           └── NotificationRecordDto.kt
│
├── feature/                              业务功能模块（按领域分包）
│   ├── appslist/
│   │   ├── AppDatabaseHolder.kt
│   │   ├── AppIconRepository.kt
│   │   ├── AppRepository.kt
│   │   ├── IconCacheManager.kt
│   │   ├── InstalledAppsRepository.kt
│   │   ├── PinnedAppsRepository.kt
│   │   ├── RemoteAppsCache.kt
│   │   ├── launch/AppLaunchManager.kt
│   │   ├── model/RemoteAppInfo.kt
│   │   └── sync/
│   │       ├── AppListSyncManager.kt
│   │       └── IconSyncManager.kt
│   ├── audio/
│   │   ├── AudioRelayPlayer.kt
│   │   └── service/AudioRelayForegroundService.kt
│   ├── clipboard/
│   │   ├── ClipboardProcessor.kt
│   │   ├── ClipboardSyncManager.kt
│   │   ├── ClipboardSyncReceiver.kt
│   │   ├── FcitxClipboardManager.kt
│   │   └── FcitxClipboardReceiver.kt
│   ├── device/
│   │   ├── model/
│   │   │   ├── DeviceModels.kt           DeviceInfo / AuthInfo / PendingPairing
│   │   │   ├── DeviceNameCache.kt
│   │   │   ├── DeviceSnapshot.kt
│   │   │   ├── HandshakeRequest.kt
│   │   │   ├── NotificationRecordStore.kt       NotificationRecordStore / NotifyRelayStoreProvider（Room 持久化单例）
│   │   │   ├── NotificationRepository.kt        NotificationRepository（门面：历史流/增删/同步/缓存）
│   │   │   ├── NotificationTextReader.kt        NotificationTextReader（文本/verify_code 读取工具）
│   │   │   └── NotificationCacheCleaner.kt      NotificationCacheCleaner（缓存清理/历史老化清理）
│   │   ├── receiver/
│   │   │   ├── FairMemoryReceiver.kt
│   │   │   └── RemoteNotificationClickReceiver.kt
│   │   ├── repository/NotificationForwardRepository.kt
│   │   └── service/
│   │       ├── DeviceConnectionManager.kt
│   │       ├── DeviceConnectionManagerSingleton.kt
│   │       ├── audio/AudioRelayController.kt
│   │       ├── callback/
│   │       │   ├── DataCallbackHandler.kt
│   │       │   ├── DeviceCallbackHost.kt          (interface)
│   │       │   ├── DeviceEventCallbackHandler.kt
│   │       │   ├── HandshakeRequestHandler.kt     (interface)
│   │       │   ├── PairingCallbackHandler.kt
│   │       │   └── RustCallbackInstaller.kt
│   │       ├── core/RustCoreSession.kt
│   │       ├── pairing/
│   │       │   ├── HandshakeWaiterRegistry.kt
│   │       │   └── PairingCoordinator.kt
│   │       ├── state/
│   │       │   ├── DeviceDirectory.kt
│   │       │   ├── DeviceQuery.kt
│   │       │   ├── DeviceSnapshotStore.kt
│   │       │   ├── DeviceTargetRegistry.kt
│   │       │   ├── OnlineDevicesCache.kt
│   │       │   └── PairedDeviceRemover.kt
│   │       ├── statequery/StateQueryResponder.kt
│   │       └── system/
│   │           ├── LegacyDeviceMigrator.kt
│   │           └── SystemStateMonitor.kt
│   ├── media/
│   │   ├── MediaControlUtil.kt
│   │   ├── MediaMessageReceiveMode.kt
│   │   ├── MediaSessionResender.kt
│   │   ├── MediaSessionTimeoutCleaner.kt
│   │   ├── MediaStateApplier.kt
│   │   ├── RemoteMediaSessionManager.kt
│   │   ├── lyric/
│   │   │   ├── LyricFieldProbe.kt
│   │   │   └── LyricResolver.kt
│   │   └── service/
│   │       ├── MediaControllerRegistry.kt
│   │       ├── MediaProjectionForegroundService.kt
│   │       ├── MediaSessionHealthMonitor.kt
│   │       └── MediaSessionMonitorService.kt
│   └── notification/
│       ├── data/ChatMemory.kt
│       ├── filter/
│       │   ├── BackendLocalFilter.kt
│       │   ├── BackendRemoteFilter.kt          （门面）
│       │   ├── RemoteFilterConfig.kt
│       │   ├── RemoteFilterDedupCache.kt
│       │   ├── RemoteFilterPendingMonitor.kt
│       │   └── RemoteFilterPlaceholderQueue.kt
│       ├── service/
│       │   ├── ListenerForegroundController.kt    （前台常驻通知与 WakeLock）
│       │   ├── MediaNotificationHandler.kt        （媒体通知处理与 MediaSession 缓存）
│       │   ├── NotificationProcessedCache.kt      （已处理通知去重缓存）
│       │   └── NotifyRelayNotificationListenerService.kt
│       └── superisland/
│           ├── config/SuperIslandConfigUtils.kt
│           ├── data/SuperIslandStructuredDataHelper.kt
│           ├── floating/
│           │   ├── FloatingComposeContainer.kt
│           │   ├── FloatingWindowContainer.kt
│           │   ├── FloatingWindowLifecycleOwner.kt
│           │   ├── FloatingWindowManager.kt
│           │   └── bigisland/
│           │       ├── ComposeFloatingView.kt
│           │       └── ParamIslandCompose.kt
│           ├── formatter/SuperIslandDataFormatter.kt
│           ├── history/SuperIslandHistoryStore.kt
│           ├── image/SuperIslandImageStore.kt
│           ├── lifecycle/LifecycleManager.kt
│           ├── media/MediaCapsulePresenter.kt
│           ├── notification/
│           │   ├── GeneralReplicaNotifier.kt
│           │   ├── LiveUpdatesIconLoader.kt
│           │   ├── LiveUpdatesIntentFactory.kt
│           │   ├── LiveUpdatesNotificationManager.kt
│           │   ├── LiveUpdatesProgressStyleBuilder.kt
│           │   ├── MediaReplicaNotifier.kt
│           │   ├── NotificationGenerator.kt              （编排门面）
│           │   ├── ReplicaIconCache.kt
│           │   ├── ReplicaIntentFactory.kt
│           │   ├── ReplicaScrollUpdater.kt
│           │   ├── ReplicaSmallIconInjector.kt
│           │   └── SuperIslandListManager.kt
│           ├── receiver/NotificationBroadcastReceiver.kt
│           ├── replica/
│           │   ├── FloatingReplicaListModeManager.kt
│           │   ├── FloatingReplicaManager.kt
│           │   ├── FloatingReplicaMappingManager.kt
│           │   ├── FloatingReplicaNotificationManager.kt
│           │   └── FloatingReplicaWindowManager.kt
│           ├── store/SuperIslandRemoteStore.kt
│           └── tracker/LocalSuperIslandTracker.kt
│
└── ui/
    ├── activity/
    │   ├── ClipboardSyncActivity.kt
    │   ├── DeveloperModeActivity.kt
    │   ├── GuideActivity.kt
    │   ├── MainActivity.kt
    │   ├── MainActivityContent.kt           主界面 Compose 内容根（主题 + Navigation3 导航图）
    │   ├── MainActivityPermissions.kt       权限校验、引导页跳转与服务启动横幅
    │   ├── ScreenCaptureCoordinator.kt      屏幕捕获授权流程协调器
    │   └── SuperIslandTestLauncher.kt       超级岛测试样本调试入口（仅 debug）
    ├── common/
    │   ├── DoubleClickConfirm.kt
    │   ├── MiuixSpinnerPreference.kt
    │   ├── NavigationEventProvider.kt
    │   ├── ScrollableTopAppBarPage.kt       统一页面根容器（AGENTS.md 约定优先复用）
    │   └── Theme.kt
    ├── dialog/
    │   ├── AddKeywordDialog.kt
    │   ├── AppPickerDialog.kt
    │   ├── ConnectDeviceDialog.kt
    │   ├── HandshakeRequestDialog.kt
    │   ├── PairingCodeDialog.kt
    │   ├── PairingCodeInputField.kt
    │   ├── RejectedDevicesDialog.kt
    │   ├── SuperIslandTestDialog.kt
    │   ├── SuperIslandTestSamples.kt
    │   └── UpdateDialog.kt
    ├── guide/
    │   ├── GuideAgreementPage.kt
    │   ├── GuideBasicSettingsPage.kt
    │   ├── GuideCompletePage.kt
    │   ├── GuideComponents.kt
    │   ├── GuideOptionalPermissionPage.kt
    │   ├── GuidePermissionUiState.kt
    │   ├── GuideRequiredPermissionPage.kt
    │   ├── GuideScreen.kt
    │   └── GuideWelcomePage.kt
    ├── navigation/
    │   ├── Navigator.kt
    │   └── Route.kt
    ├── pages/
    │   ├── ChatTest.kt
    │   ├── ClipboardSyncPage.kt
    │   ├── DeleteButton.kt                  滑动删除按钮（原 ui/pages/history/DeleteButton.kt 上提）
    │   ├── FilterListSection.kt
    │   ├── MusicControlPage.kt
    │   ├── NotificationHistory.kt
    │   ├── RemoteAppsPage.kt
    │   ├── TimeFormatters.kt                时间戳/时长格式化
    │   ├── remoteapps/
    │   │   ├── AppGridItems.kt
    │   │   ├── DisplayInfo.kt
    │   │   ├── DisplayNavigationBar.kt
    │   │   ├── LocalAppsContent.kt
    │   │   ├── RemoteAppsContent.kt
    │   │   ├── RemoteAppsMenuHost.kt
    │   │   └── RemoteAppsPageState.kt
    │   ├── SuperIslandHistory.kt
    │   ├── superisland/
    │   │   ├── SuperIslandHistoryCards.kt
    │   │   ├── SuperIslandHistoryFormatters.kt
    │   │   ├── SuperIslandHistoryImages.kt
    │   │   └── SuperIslandImageCache.kt
    │   ├── SuperIslandSettings.kt
    │   ├── UIAbout.kt
    │   ├── UIAppearance.kt
    │   ├── UILocalFilter.kt
    │   ├── UIRemoteFilter.kt
    │   └── history/
    │       ├── NotificationCard.kt
    │       ├── NotificationHistoryScaffold.kt
    │       └── NotificationListBlock.kt
    ├── screen/
    │   ├── devicelist/
    │   │   ├── DeviceListButtons.kt
    │   │   ├── DeviceListDialogs.kt
    │   │   ├── DeviceListScreenState.kt
    │   │   └── GlobalSelectedDeviceHolder.kt
    │   ├── DeviceForwardScreen.kt
    │   ├── DeviceListScreen.kt
    │   ├── HistoryScreen.kt
    │   ├── MainScreen.kt
    │   └── SettingsScreen.kt
    └── viewmodel/
        ├── AppIconPreloader.kt              历史列表图标批量预加载（两个 History ViewModel 共用）
        ├── LocalAppsViewModel.kt
        ├── NotificationHistoryUiState.kt
        ├── NotificationHistoryViewModel.kt
        ├── NotificationPagingSource.kt
        ├── RemoteAppsViewModel.kt
        ├── SuperIslandHistoryUiState.kt
        ├── SuperIslandHistoryViewModel.kt
        └── SuperIslandPagingSource.kt
```

> 变更提示：早期版本中的 `servers/` 包（`servers/appslist`、`servers/clipboard`、通知监听服务等）已整体迁移至 `feature/` 下按领域分包；`feature/device/service/` 下新增 `callback/`、`core/`、`pairing/`、`state/`、`statequery/`、`system/` 等子包以拆分 `DeviceConnectionManager`。

### 2.2 `:base` 模块（17 个 .kt）

```
base/src/main/java/notifyrelay/base/util/
├── BatteryIconConverter.kt       电量图标/颜色（返回 Compose Color）   ← 原 :core
├── BatteryUtils.kt               电量与充电状态                        ← 原 :core
├── ClipboardUtils.kt             剪贴板写入封装
├── DeviceUtils.kt                平板判断、本机设备名
├── DigestUtils.kt                顶层扩展 ByteArray.toHex()
├── GuidePermissionRequester.kt   引导页权限请求封装
├── IntentUtils.kt                Intent 构造与安全启动
├── Logger.kt                     Timber 封装
├── MeasureTime.kt                耗时统计
├── PermissionHelper.kt           权限检测/申请（含前台状态监听）
├── SecureKeyStorage.kt           密钥安全存储（Android Keystore）      ← 原 :core
├── ServiceManager.kt             服务启动/运行状态检查                 ← 原 :core
├── SystemBarUtils.kt             状态栏/导航栏配色（由 :app ui/common 下沉）
├── TextUtils.kt                  标题前缀归一（normalizeTitle）
├── ThemeSettingsManager.kt       主题基色索引与变更监听
├── ToastUtils.kt                 Toast 封装
└── image/ImageUtils.kt           图片处理工具（DataUrl/Coil/颜色解析/HTML 反转义/位图转换） ← 原 :core

```

> 变更提示：原 `:core` 模块（包 `notifyrelay.core.util[.image]`）已整体并入本模块，
> 包名改为 `notifyrelay.base.util[.image]`，模块与其 Gradle 坐标撤销。
> 随之新增依赖：`androidx.compose.ui`（`BatteryIconConverter.getBatteryColor` 返回 `Color`）
> 与 `libs.bundles.coil`（`ImageUtils.loadBitmap`）。
> 原 `:core` 的 `MediaStoreHelper.kt` 因全仓无调用点而删除。
> 原先的 `EncryptionManager.kt` 已移除，加密/解密职责上移至 `:core`（Rust 侧 ECDH），
> 本地密钥存储由 `SecureKeyStorage` 承担。

### 2.3 `:data` 模块（32 个 .kt）

```
data/src/main/java/notifyrelay/data/
├── PersistenceManager.kt                 通知记录 JSON 文件读写、本机 UUID
├── StorageManager.kt                     SharedPreferences 封装（多 PrefsType + 迁移）
├── FilterConfigDefaults.kt               过滤规则默认配置
├── config/
│   ├── DeviceInfoManager.kt              设备信息文件生成
│   ├── ScrcpyDefaults.kt
│   ├── ScrcpyPreferenceKeys.kt
│   └── ScrcpyPresets.kt
├── model/ScrcpyModels.kt                 ConnectionTarget / OnlineDeviceInfo / SelectedDeviceInfo
└── database/
    ├── AppDatabase.kt                    Room 数据库（version 9，13 张表，8 个 Migration）
    ├── dao/
    │   ├── AppConfigDao.kt
    │   ├── AppDao.kt
    │   ├── AppDeviceDao.kt
    │   ├── FilterListDao.kt              黑名单/白名单/过滤条目/包名分组
    │   ├── NotificationRecordDao.kt
    │   ├── SuperIslandHistoryDao.kt
    │   ├── SuperIslandImageDao.kt
    │   └── SuperIslandMirrorFilterDao.kt
    ├── entity/
    │   ├── AppConfigEntity.kt
    │   ├── AppDeviceEntity.kt
    │   ├── AppEntity.kt
    │   ├── BlackListEntryEntity.kt
    │   ├── FilterEntryEntity.kt
    │   ├── NotificationRecordEntity.kt
    │   ├── PackageGroupEntity.kt         （同文件内含 PackageGroupItemEntity）
    │   ├── SuperIslandHistoryEntity.kt
    │   ├── SuperIslandHistorySummary.kt
    │   ├── SuperIslandImageBindingEntity.kt
    │   ├── SuperIslandImageEntity.kt
    │   ├── SuperIslandMirrorFilterEntity.kt
    │   └── WhiteListEntryEntity.kt
    ├── migration/MigrationHelper.kt
    └── repository/DatabaseRepository.kt  统一 DAO 门面（100+ 方法）
```

### 2.4 `:superislandui` 模块（60 个 .kt）

```
superislandui/src/main/java/github/xzynine/superislandui/
├── common/
│   ├── BitmapUtils.kt
│   ├── CapsuleScrollManager.kt
│   ├── PreviewData.kt
│   ├── SuperIslandManager.kt        启用检测、协议版本、权限
│   ├── SuperIslandProtocol.kt       State / Diff / Payload 构建
│   ├── SuperIslandTextComponents.kt
│   └── TextSplitter.kt
├── diff/DiffSystem.kt               增量差分
├── parser/
│   ├── AParser.kt
│   └── BParser.kt
├── model/
│   ├── components/
│   │   ├── ActionInfo.kt
│   │   ├── AnimTextInfoData.kt       AnimIconInfo / AnimTextInfo
│   │   ├── MediaSessionData.kt
│   │   ├── MultiProgressData.kt      MultiProgressInfo
│   │   ├── ProgressInfo.kt
│   │   ├── TextButtonData.kt         TextButton
│   │   └── TimerInfo.kt              TimerInfo
│   ├── core/
│   │   ├── ParamIslandData.kt        ParamIsland / SmallIslandArea
│   │   ├── ParamV2.kt
│   │   └── SuperIslandData.kt
│   └── templates/
│       ├── BaseInfo.kt
│       ├── ChatInfo.kt
│       ├── HighlightInfo.kt
│       ├── HintInfo.kt
│       └── PicInfo.kt
└── floating/
    ├── common/
    │   ├── CommonCompose.kt
    │   ├── FocusIconResolver.kt
    │   └── SuperIslandImageUtil.kt
    ├── bigisland/components/
    │   ├── ActionCompose.kt
    │   ├── AnimTextInfoCompose.kt
    │   ├── BaseInfoCompose.kt
    │   ├── ChatInfoCompose.kt
    │   ├── DefaultCompose.kt
    │   ├── HighlightInfoCompose.kt
    │   ├── HintInfoCompose.kt
    │   ├── MediaIslandCompose.kt
    │   ├── MultiProgressCompose.kt
    │   ├── PicInfoCompose.kt
    │   ├── ProgressCompose.kt
    │   ├── ProgressInfoCompose.kt
    │   ├── TextButtonCompose.kt
    │   └── TimerInfoCompose.kt
    └── smallisland/
        ├── core/BigIslandArea.kt
        ├── left/AComponent.kt            AImageText1 / AImageText5
        ├── right/BComponent.kt           BImageText2/3/4/6、BTextInfo、BPicInfo、BProgressTextInfo 等
        └── compose/
            ├── ACompose.kt
            ├── BCompose.kt
            └── BigIslandCollapsedCompose.kt
```

> 变更提示：`componets` 拼写已修正为 `components`；`floating/BigIsland`、`floating/SmallIsland` 目录名改为小写 `bigisland` / `smallisland`；`model/core/ParamV2Renderer.kt` 更名为 `ParamV2.kt`；模板模型去掉 `Data` 后缀（`BaseInfoData.kt` → `BaseInfo.kt`）；新增 `diff/` 与 `parser/`。

### 2.5 `:checkupdata` 模块（10 个 .kt，Git 子模块）

```
checkupdata/src/main/java/github/xzynine/checkupdata/
├── CheckUpdateManager.kt          更新检查入口
├── api/
│   ├── GitHubApiClient.kt
│   └── GitHubApiService.kt
├── cache/UpdateCache.kt
├── download/SystemDownloader.kt
├── model/
│   ├── CheckUpdateConfig.kt
│   ├── ReleaseInfo.kt
│   └── UpdateResult.kt             sealed class
└── version/
    ├── VersionComparator.kt
    └── VersionRule.kt              enum class
```

### 2.6 `:scrcpy` 模块（53 个 .kt，Git 子模块）

```
scrcpy/src/main/java/io/github/miuzarte/scrcpyforandroid/
├── NativeCoreFacade.kt
├── ScrcpySessionInfo.kt
├── constants/            Defaults.kt / UiAndroidKeycodes.kt / UiMotion.kt / UiSpacing.kt
├── haptics/              AppHaptics.kt
├── nativecore/           AdbMdnsDiscoverer.kt / AdbPairingTypes.kt / AnnexBDecoder.kt
│                         DirectAdbClient.kt / DirectAdbPairingClient.kt
│                         NativeAdbService.kt / PersistentVideoRenderer.kt / ScrcpyAudioPlayer.kt
├── pages/                AdvancedConfigPage.kt / DevicePage.kt / ImeTextSubmission.kt
│                         ScrcpyScreenHost.kt / ScrcpyUiEnvironment.kt / ScrcpyUiViewModel.kt
│                         SettingsPage.kt / ShortcutLaunchActivity.kt / VirtualButtonOrderPage.kt
├── scaffolds/            MiuixSpinnerCompat.kt / PageLayouts.kt / SuperSlide.kt
├── scrcpy/               ClientOptions.kt / Scrcpy.kt / ScrcpyUsageExample.kt
│                         ServerParams.kt / Shared.kt / TouchEventHandler.kt
├── services/             AppScreenOn.kt / AppSettingsStore.kt / AppWakeLocks.kt
│                         AudioForwardingService.kt / DeviceConnectionLogic.kt
│                         DynamicShortcutManager.kt / LocalInputService.kt
│                         NativeAacRecorder.kt / NativeAudioRecorder.kt / NativeMp4Recorder.kt
│                         NativeRecordingSupport.kt / NativeWavRecorder.kt
│                         PinShortcutManager.kt / QuickDeviceStore.kt
└── widgets/              DeviceWidgets.kt / ReorderableList.kt / ScrcpyInputSurfaceView.kt
                          SectionSmallTitle.kt / StatusCardModels.kt / VirtualButtons.kt
```

### 2.7 `:core` 模块（2 个 .kt + Rust 子模块）

```
nativecore/src/main/java/com/xzyht/notifyrelay/nativecore/
├── NativeCore.kt         JNA 绑定：Rust 核心全部导出函数（会话、加密、配对、心跳、
│                         应用列表同步、剪贴板、超级岛/媒体状态推送、FTP 凭据派生等）
└── NotifyRelayCore.kt    JNA 库加载与指针→字符串辅助（ptrToStringAndFree）
```

> 模块名与目录：本模块 Gradle 坐标为 `:core`（占用原 `notifyrelay.core` 工具库让出的名字），
> 源码目录仍为 `nativecore/`，`notify-relay-core` 子模块路径不变
> （`settings.gradle.kts` 中以 `projectDir` 重定向）。namespace 保持 `com.xzyht.notifyrelay.nativecore`。

> 原 `:lsp` 模块（`XposedInit.kt` / `XmsfAuthFix.kt`）已迁至独立仓库 **`NotifyRelay-LSP`**，
> 见该仓库的 `app/src/main/java/com/xzyht/notifyrelay/lsp/`。

---

## 三、主要类的职责与公开方法

### 3.1 `:base`

| 文件 | 类/对象 | 主要公开方法 |
|---|---|---|
| `DeviceUtils.kt` | `object DeviceUtils` | `isTablet(context)`, `getLocalDeviceName(context)`（`sanitizeDisplayName` 为私有） |
| `ClipboardUtils.kt` | `object ClipboardUtils` | `copyText(context, label, text)`（写系统剪贴板，失败返回 false） |
| `DigestUtils.kt` | 顶层 `fun ByteArray.toHex(): String` | — |
| `GuidePermissionRequester.kt` | `class GuidePermissionRequester` | `requestPostNotifications()`, `requestQueryApps()`, `requestLocalNetwork()`, `requestBluetoothConnect()` |
| `IntentUtils.kt` | `object IntentUtils` | `startActivity(...)`(2 重载), `createIntent<T>()`, `createImplicitIntent(action)` |
| `Logger.kt` | `object Logger`（Timber 封装） | `v/d/i/w/e()` 各 2 个重载（默认 Tree 在 `init` 块内 `Timber.plant`） |
| `MeasureTime.kt` | 顶层 `inline fun <T> measureTime(...)` | — |
| `PermissionHelper.kt` | `object PermissionHelper` | `checkAllPermissions()`, `detectMiuiOrPengpai(context)`, `requestSensitiveNotificationPermission()`, `checkBluetoothConnectPermission()`, `checkOverlayPermission()`, `requestOverlayPermission()`, `checkBackgroundUnlimitedPermission()`, `checkNotificationListenerServiceCanStart()`, `getDeclaredPermissions()`, `isLocalNetworkPermissionRequired()`, `checkLocalNetworkPermission()`, `getApplicableDeclaredPermissions()`, `openAppDetailsSettings()`, `checkManageExternalStoragePermission()`, `requestManageExternalStoragePermission()`, `getDetailedOsVersion()`, `isVersionGreaterThan(...)`, `isDeviceLocked()`, `isAppInForeground()`；内部 `object AppForegroundDetector` 提供 `initialize()`, `isForeground()`, `addListener()`, `removeListener()` |
| `SystemBarUtils.kt` | `object SystemBarUtils` | `setStatusBarColor(window, color, animate)`, `setNavigationBarColor(window, color, animate)`（反射降级，纯 Android 平台工具） |
| `TextUtils.kt` | `object TextUtils` | `normalizeTitle(title: String?)` —— 去掉形如 `(xxx)` 的前缀并 trim |
| `ThemeSettingsManager.kt` | `object ThemeSettingsManager` | `fun interface ThemeChangeListener`, `getThemeBaseIndex(context)`, `setThemeBaseIndex(...)`, `addThemeChangeListener(...)`, `removeThemeChangeListener(...)` |
| `ToastUtils.kt` | `object ToastUtils` | `showShortToast(...)`, `showLongToast(...)` |

> 由上表起以下 4 个文件原属 `:core` 模块，已并入 `:base`（包 `notifyrelay.base.util`）：

| 文件 | 类/对象 | 主要公开方法 |
|---|---|---|
| `BatteryIconConverter.kt` | `object BatteryIconConverter` | `getBatteryIcon(...)`, `getBatteryColor(batteryLevel)` |
| `BatteryUtils.kt` | `object BatteryUtils` | `getBatteryLevel(context)`, `isCharging(context)` |
| `SecureKeyStorage.kt` | `object SecureKeyStorage` | `encryptAndStore(...)`, `retrieveAndDecrypt(...)`, `removeKey(...)` |
| `ServiceManager.kt` | `object ServiceManager` | `startNotificationListenerService(context)`, `startAllServices(context)`（服务类名以常量 `NOTIFICATION_LISTENER_SERVICE_CLASS` 持有：`:base` 不能反向依赖 `:app`，故无法引用服务类） |
| `image/ImageUtils.kt` | `object ImageUtils` + 顶层扩展 | `isDataUrl(text)`, `decodeDataUrlToBitmap(...)`, `bitmapToBase64(bitmap)`, `bitmapToDataUri(bitmap)`, `bytesToDataUrl(bytes, mime)`, `loadBitmap(...)`, `parseColor(colorString)`, `unescapeHtml(input)`, `scaleDown(bitmap, maxDimension)`；顶层扩展 `Drawable.toBitmapOrDefault(fallbackSize)` |


### 3.2 `:data`

| 文件 | 类/对象 | 主要公开方法 |
|---|---|---|
| `PersistenceManager.kt` | `object PersistenceManager` | `readNotificationRecords<T>(...)`, `getAllNotificationFiles(context)`, `getLocalDeviceUuid(context)` |
| `StorageManager.kt` | `object StorageManager` | `getString/putString`, `getBoolean/putBoolean/putBooleans`, `getInt/putInt/putInts`, `getFloat/putFloat`, `getLong/putLong`, `putStrings`, `getStringSet/putStringSet`, `remove`, `clear`, `migrateData`；按 `PrefsType`（`GENERAL`/`DEVICE`/`FILTER`）自动加键前缀 |
| `FilterConfigDefaults.kt` | `object FilterConfigDefaults` | 过滤规则默认常量 |
| `config/DeviceInfoManager.kt` | `object DeviceInfoManager` | `generateDeviceInfoFile(context)` |
| `config/ScrcpyDefaults.kt` | `object ScrcpyDefaults` | Scrcpy 配置项默认值 |
| `config/ScrcpyPreferenceKeys.kt` | `object ScrcpyPreferenceKeys` | Scrcpy 偏好键常量 |
| `config/ScrcpyPresets.kt` | `object ScrcpyPresets` | Scrcpy 预设 |
| `model/ScrcpyModels.kt` | `ConnectionTarget`, `OnlineDeviceInfo`, `SelectedDeviceInfo` | 数据模型 |
| `database/AppDatabase.kt` | `abstract class AppDatabase : RoomDatabase` | `appConfigDao()`, `appDao()`, `appDeviceDao()`, `notificationRecordDao()`, `superIslandHistoryDao()`, `superIslandImageDao()`, `superIslandMirrorFilterDao()`, `filterListDao()`；`getDatabase(context)`；迁移 `MIGRATION_1_2` … `MIGRATION_8_9`；`migrateFromLegacyStorage(...)` |
| `dao/AppConfigDao.kt` | `interface AppConfigDao` | `getAll()`, `getValue(key)`, `insert(config)`, `insertAll(configs)`, `delete(config)`, `deleteByKey(key)` |
| `dao/AppDao.kt` | `interface AppDao` | `getAll(): Flow`, `getByPackageName`, `getByPackageNames`, `insert`, `insertAll`, `delete`, `deleteByPackageName`, `getIconMissingApps(): Flow`, `updateIcon`, `markIconAsMissing`, `getExpiredApps(expiryTime)` |
| `dao/AppDeviceDao.kt` | `interface AppDeviceDao` | `getAll(): Flow`, `getByPackageName(): Flow`, `getByPackageNames`, `getByDeviceUuid(): Flow`, `getByPackageNameAndDeviceUuid`, `insert`, `insertAll`, `delete`, `deleteByPackageName`, `deleteByDeviceUuid`, `deleteByPackageNameAndDeviceUuid` |
| `dao/FilterListDao.kt` | `interface FilterListDao` | 黑/白名单、过滤条目、包名分组的查询与 `replace*` 批量替换 |
| `dao/NotificationRecordDao.kt` | `interface NotificationRecordDao` | `getByDevice`, `getAll`, `getByKey`, `insert`, `insertAll`, `delete`, `deleteByKey`, `deleteByDevice`, `deleteOldRecords`, `countByDevice`, `countByPackageAndDevice`, `deleteByPackageAndDevice`, `getByPackageAndDevice`, `deleteOldestByPackageAndDevice`, `getPagingSourceByDevice`, `getPackageCountByDevice` |
| `dao/SuperIslandHistoryDao.kt` | `interface SuperIslandHistoryDao` | `getAllHistory`, `getAllHistorySummary`, `upsertByFeatureAndContent`, `getLatestByDistinctFeatureId`, `getPackageCount`, `deleteByPackage`, `getCount` 等 20+ 方法 |
| `dao/SuperIslandImageDao.kt` | `interface SuperIslandImageDao` | `getImageIdByHash`, `getImageDataById`, `getImageDataByBinding`, `insertImage`, `upsertBinding`, `touchImage`, `deleteImagesOlderThan`, `deleteImagesKeepingLatest`, `clearAllBindings`, `clearAllImages` |
| `dao/SuperIslandMirrorFilterDao.kt` | `interface SuperIslandMirrorFilterDao` | `getEnabledPackages`, `getAllPackages`, `upsert`, `setEnabled` |
| `migration/MigrationHelper.kt` | `object MigrationHelper` | `migrateAppConfig`, `migrateNotifications`, `migrateSuperIslandHistory`, `migrateApps`（均为 `suspend`）, `cleanupLegacyStorage(context)`, `shouldMigrate(context)` |
| `repository/DatabaseRepository.kt` | `class DatabaseRepository` | 全部 DAO 的门面封装（100+ 方法，含 `getBlackList/replaceBlackList`、`getWhiteList/replaceWhiteList`、`getLocalFilterEntries/replaceLocalFilterEntries`、`getPackageGroups/replacePackageGroups`、超级岛图片/历史/镜像过滤等） |

### 3.3 `:superislandui`

| 类 | 职责 |
|---|---|
| `SuperIslandManager` | 悬浮岛启用检测、协议版本读取、权限判断 |
| `SuperIslandProtocol` | State / Diff / Payload 的构建与解析 |
| `DiffSystem` | 状态增量差分 |
| `TextSplitter` | 文本按宽度/规则分割 |
| `CapsuleScrollManager` | 胶囊滚动管理 |
| `BitmapUtils` | Bitmap 处理（缩放、圆角、缓存等） |
| `PreviewData` | 预览数据 |
| `SuperIslandTextComponents` | 文本组件：`AutoFitText`、`AutoScrollText` |
| `AParser` / `BParser` | 小岛 A/B 区组件解析器 |
| `SuperIslandImageUtil` | 图片加载与变换 |
| `FocusIconResolver` | 焦点图标解析 |
| `CommonCompose` | 通用 Compose 组件 |
| `ParamV2` / `ParamIsland` / `SuperIslandData` | 核心数据模型 |
| `BaseInfo` / `ChatInfo` / `HighlightInfo` / `HintInfo` / `PicInfo` | 模板数据模型 |
| `ActionInfo` / `AnimTextInfo` / `MediaSessionData` / `MultiProgressInfo` / `ProgressInfo` / `TextButton` / `TimerInfo` | 组件数据模型 |
| `BigIslandArea` / `ACompose` / `BCompose` / `BigIslandCollapsedCompose` | 小岛区域与折叠态渲染 |
| `bigisland/components/*Compose` | 大岛 14 个具体组件渲染 |

### 3.4 `:checkupdata`

| 类 | 职责 |
|---|---|
| `CheckUpdateManager` | 更新检查入口，编排请求/缓存/下载 |
| `GitHubApiClient` / `GitHubApiService` | GitHub Release API 客户端与接口定义 |
| `UpdateCache` | 更新检查结果缓存 |
| `SystemDownloader` | 调用系统下载器下载 APK |
| `CheckUpdateConfig` / `ReleaseInfo` / `UpdateResult` | 配置、Release 信息、结果（sealed） |
| `VersionComparator` / `VersionRule` | 版本号比较与规则 |

### 3.5 `:core`

`NativeCore`（object）是 Rust 核心 `notify-relay-core` 的 JNA 绑定门面，覆盖：

- 会话：`createContext()`, `exportState()`, `importState()`, `encryptLocalState()`, `decryptLocalState()`
- 加密：`generateKeypair()`, `getPublicKey()`, `hasKeypair()`, `deriveSharedSecret()`, `migrateSharedSecret()`, `deriveFtpCredentials()`, `derivePasswordHash()`, `generateRandomPassword()`
- 设备：`getLocalUuid()`, `renameDevice()`, `getDeviceList()`, `removeDevice()`, `removeDeviceSession()`
- 连接/配对：`periodicBroadcast()`, `sendHandshake()`, `connectDevice()`, `sendPairingInit()`, `sendPairingResp()`, `sendAccept()`, `sendReject()`, `generatePairingCode()`, `clearPairingCode()`
- 消息：`enqueueMessage()`, `computeDedupKey()`, `computeFeatureId()`, `dedup()`, `shouldDeduplicate()`
- 过滤：`setFilterConfig()`, `mapLocalPackage()`, `checkFilterMode()`
- 同步：`clipboardOnChanged()`, `clipboardOnReceived()`, `appSyncPrepareIconRequest()`, `appSyncClearIconPending()`, `appSyncParseIconResponse()`, `appSyncBuildApplistRequest()`, `appSyncParseApplistResponse()`
- 状态推送：`pushSuperislandState()`, `pushMediaState()`, `setOnStateQueryCallback()`, `onNetworkChanged()`, `getLocalIp()`

`NotifyRelayCore` 提供 JNA 库加载与 `ptrToStringAndFree()` 辅助。

### 3.6 `:lsp`（已迁出本仓库）

LSPosed 模块现位于独立仓库 **`NotifyRelay-LSP`**（独立 APK，headless、无 GUI），
不参与本仓库构建，源码见该仓库 `app/src/main/java/com/xzyht/notifyrelay/lsp/`：
- `XposedInit` — LSPosed 模块入口（继承 `XposedModule`），负责作用域判定与 Hook 安装。
- `XmsfAuthFix` — 超级岛鉴权绕过实现。

### 3.7 `:app` — 关键类速查

| 包 | 类 | 职责 |
|---|---|---|
| `util` | `ApkArchMatcher` | 依据设备 ABI 匹配更新包 |
| `sync` | `ConnectionDiscoveryManager` | 局域网设备发现 |
| `sync` | `ConnectionKeepAlive` | 保活与重连 |
| `sync` | `FtpServerManager` / `FtpServerInfo` | 内置 FTP 服务（图标/文件分发） |
| `sync` | `HeartbeatProcessor` / `ProtocolSender` / `MessageSender` | 心跳、协议封装、消息发送（`MessageSender` 为门面，`SendGateway` 与 `builder/*` 为具体实现） |
| `sync/notification` | `NotificationProcessor` / `StatusProcessor` / `SuperIslandProcessor` | 三类协议的处理 |
| `feature/appslist` | `AppRepository`（门面）/ `AppDatabaseHolder` / `InstalledAppsRepository` / `AppIconRepository` / `PinnedAppsRepository` / `RemoteAppsCache` / `IconCacheManager` / `AppListSyncManager` / `IconSyncManager` / `AppLaunchManager` | 应用列表采集、图标缓存、跨端同步与远程拉起 |
| `feature/clipboard` | `ClipboardSyncManager` / `ClipboardProcessor` / `ClipboardSyncReceiver` / `FcitxClipboardManager` / `FcitxClipboardReceiver` | 剪贴板同步（含 Fcitx 输入法适配） |
| `feature/audio` | `AudioRelayPlayer` / `AudioRelayForegroundService` / `AudioRelayController` | 音频转发播放 |
| `feature/device` | `DeviceConnectionManager`(+`Singleton`) | 设备连接总控 |
| `feature/device/service/core` | `RustCoreSession` | 封装 `NativeCore` 会话 |
| `feature/device/service/callback` | `RustCallbackInstaller` / `DataCallbackHandler` / `DeviceEventCallbackHandler` / `PairingCallbackHandler` / `DeviceCallbackHost` / `HandshakeRequestHandler` | Rust 回调分发 |
| `feature/device/service/pairing` | `PairingCoordinator` / `HandshakeWaiterRegistry` | 配对流程编排 |
| `feature/device/service/state` | `DeviceDirectory` / `DeviceQuery` / `DeviceSnapshotStore` / `DeviceTargetRegistry` / `OnlineDevicesCache` / `PairedDeviceRemover` | 设备状态存储与查询 |
| `feature/device/service/system` | `SystemStateMonitor` / `LegacyDeviceMigrator` | 系统状态监听与旧数据迁移 |
| `feature/media` | `MediaControlUtil` / `RemoteMediaSessionManager` / `MediaSessionMonitorService` / `MediaProjectionForegroundService` | 媒体会话监听与远程控制 |
| `feature/media/lyric` | `LyricFieldProbe` / `LyricResolver` | 歌词字段探测与歌词状态解析 |
| `feature/media/service` | `MediaControllerRegistry` / `MediaSessionHealthMonitor` | 媒体控制器注册表、会话健康检查与重试 |
| `feature/media`（拆分新增） | `MediaMessageReceiveMode` / `MediaSessionTimeoutCleaner` / `MediaSessionResender` / `MediaStateApplier` | 接收模式枚举、超时会话清理与复传调度、媒体状态构建与应用。`MediaSessionTimeoutCleaner.host` 由 `onMediaMessageReceived` 首次调用时惰性执行 `RemoteMediaSessionManager.init()` 完成 `bind()`，保证超时清理链路生效。 |
| `feature/notification` | `NotifyRelayNotificationListenerService` / `ListenerForegroundController` / `MediaNotificationHandler` / `NotificationProcessedCache` / `HighPriorityNotifier` | 系统通知监听入口、前台通知与 WakeLock、媒体通知处理、已处理通知去重缓存、本地高优先级悬浮通知（5s 自动取消） |
| `feature/notification/filter` | `BackendLocalFilter` / `BackendRemoteFilter`（门面）/ `RemoteFilterConfig` / `RemoteFilterDedupCache` / `RemoteFilterPlaceholderQueue` / `RemoteFilterPendingMonitor` | 本地/远端通知过滤规则、去重缓存、延迟复刻占位队列与待撤回监控（标题前缀归一转由 `:base` 的 `TextUtils.normalizeTitle` 提供） |
| `feature/notification/superisland` | `SuperIslandConfigUtils` / `SuperIslandStructuredDataHelper` / `SuperIslandDataFormatter` / `SuperIslandHistoryStore` / `SuperIslandImageStore` / `SuperIslandRemoteStore` / `LocalSuperIslandTracker` | 超级岛数据格式化、历史与图片存储、远端状态 |
| `feature/notification/superisland/floating` | `FloatingWindowManager` / `FloatingWindowContainer` / `FloatingComposeContainer` / `FloatingWindowLifecycleOwner` / `ComposeFloatingView` / `ParamIslandCompose` | 悬浮窗管理与 Compose 渲染 |
| `feature/notification/superisland/notification` | `NotificationGenerator`（编排门面）/ `MediaReplicaNotifier` / `GeneralReplicaNotifier` / `ReplicaIconCache` / `ReplicaIntentFactory` / `ReplicaScrollUpdater` / `ReplicaSmallIconInjector` / `LiveUpdatesNotificationManager` / `LiveUpdatesProgressStyleBuilder` / `LiveUpdatesIconLoader` / `LiveUpdatesIntentFactory` / `SuperIslandListManager` | 实况通知生成与列表管理 |
| `feature/notification/superisland/replica` | `FloatingReplicaManager` / `FloatingReplicaWindowManager` / `FloatingReplicaMappingManager` / `FloatingReplicaNotificationManager` / `FloatingReplicaListModeManager` | 悬浮岛镜像副本（多窗口/列表模式） |
| `ui/activity` | `MainActivity`(+`MainActivityContent` / `MainActivityPermissions` / `ScreenCaptureCoordinator` / `SuperIslandTestLauncher`) / `GuideActivity` / `DeveloperModeActivity` / `ClipboardSyncActivity` | 四大 Activity 及主界面拆分组件 |
| `ui/navigation` | `Navigator` / `Route` | Navigation3 导航 |
| `ui/screen` | `DeviceListScreen` / `DeviceForwardScreen` / `HistoryScreen` / `SettingsScreen` / `MainScreen` | 主界面四个 Tab 与容器 |
| `ui/screen/devicelist` | `DeviceListScreenState` / `GlobalSelectedDeviceHolder` / `DeviceListButtons`（含 `DeviceListScreenContent`） / `DeviceListDialogs` | 设备列表状态持有、按钮组件与对话框 |
| `ui/pages` | `NotificationHistory` / `SuperIslandHistory` / `RemoteAppsPage` / `MusicControlPage` / `ClipboardSyncPage` / `SuperIslandSettings` / `UILocalFilter` / `UIRemoteFilter` / `FilterListSection` / `UIAbout` / `UIAppearance` / `ChatTest` / `DeleteButton`（上提自 `ui/pages/history`）/ `TimeFormatters` | 二级页面 |
| `ui/pages/history` | `NotificationListBlock` / `NotificationCard` / `NotificationHistoryScaffold` | 通知历史页的列表块、卡片与脚手架 |
| `ui/pages/remoteapps` | `DisplayInfo` / `RemoteAppsPageState` / `RemoteAppsMenuHost` / `LocalAppsContent` / `RemoteAppsContent` / `DisplayNavigationBar` / `AppGridItems` | `RemoteAppsPage` 拆分出的显示器模型、页面状态、菜单宿主、本地/远程应用网格、多显示器底栏与网格单元 |
| `ui/pages/superisland` | `SuperIslandHistoryListBlock` / `SuperIslandHistoryGroupCard` / `SuperIslandHistorySummaryRow` / `SuperIslandHistoryStoreEntryCard` / `SuperIslandHistoryImage` / `rememberAppIconBitmap` / `SuperIslandAppIcon` / `SuperIslandImageCache` / `formatTimestamp` / `buildEntryCopyText` / `copyEntryToClipboard` / `triggerFloatingReplica` / `sanitizeImageContent` | 超级岛历史页拆分产物：卡片、图片图标、格式化工具与图片缓存 |
| `ui/guide` | `GuideScreen` + `GuideWelcomePage` / `GuideAgreementPage` / `GuideRequiredPermissionPage` / `GuideOptionalPermissionPage` / `GuideBasicSettingsPage` / `GuideCompletePage` / `GuideComponents` / `GuidePermissionUiState` | 首次启动引导 |
| `ui/viewmodel` | `*ViewModel` / `*UiState` / `*PagingSource` / `AppIconPreloader` | UI 状态与 Paging3 数据源、历史列表图标批量预加载 |
| `ui/common` | `ScrollableTopAppBarPage` / `Theme` / `DoubleClickConfirm` / `NavigationEventProvider` / `MiuixSpinnerPreference` | 通用 UI 组件与工具（`SystemBarUtils` 已下沉至 `:base`） |

---

## 四、构建相关

- 版本号来自根目录 `version.properties`（由 `update.yml` 在 push 到 `main` 时按 conventional commits 语义回写）；CI 可用 `-PversionName/-PversionCode` 或环境变量 `VERSION_NAME/VERSION_CODE` 覆盖。
- `:app` 在 release 构建时启用 ABI splits（`arm64-v8a`、`x86_64` + universal），debug 仅生成 universal APK。
- `preBuild` 依赖 `generatePermissionNotes`：解析 `AndroidManifest.xml` 中 `<uses-permission>` 上方注释，生成 `res/raw/permission_notes.txt`，供协议页（`GuideAgreementPage`）展示权限用途。**权限用途只需维护 Manifest 注释，不要在 Kotlin 中手抄**。
- `:app` 声明 `aidl = true`、`buildConfig = true`、Compose 开启。LSPosed 入口声明（`META-INF/xposed/*`）不再合并进主 APK——该模块已拆分为独立仓库 `NotifyRelay-LSP`。
- ktlint 应用于除 `checkupdata`、`scrcpy` 之外的所有模块。
